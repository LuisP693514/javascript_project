console.log("Im in the moving obj file")

class MovingObj {
    constructor(options){
        this.pos = options.pos;
        this.x = options.x;
        this.y = options.y;
    }

    
}

module.exports = MovingObj;